# IMDb Server Configuration

# Server mode: "development" or "production"
# production = uses Waitress WSGI server (recommended for deployment)
# development = uses Flask dev server with auto-reload
FLASK_ENV=production

# Port to run the server on
PORT=5001
